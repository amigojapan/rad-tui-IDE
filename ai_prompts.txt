make the bevel of the button reverse colors during white the mouse button is held
make the properties window also display the properties including the caption for the form in the properties window when the background of the form is clicked


change the properties of option buttons and check boxes, so that instead of displaying True or False in the properties window, the value can be changed using a checkbox make it so that if the form is made too small to hold the controls inside of it, it hides the portion of the controls that have been hidden by the window, and later if it is made bigger, it should reveal them agStan. 


task1
implementt button_down() function for he user to code in the code window. 
task2
implement the timer control, thatkets the user put a time in milliseconds, and every such milliseconds it calls a function

task3
make an example minesweeper for this system 10 by 10 buttons with random 10 bombs, where when the button_down function is held down, a caption at the top of the minesweeper has a (.O.) expresión kaomji in a text box bombs when the3 button held down, when the button  is released, the expression should change to a happy kaomoji if it is not a bomb button and a sad kaomoji if it is a bomb, also put a textbox with the time elapsed since the game started, and I would like this to update every second using the timer control function

--done

add an on_button_up() event. 
add a on_right_click() event. 
make the background color propertie of buttons settabl. 

now, for a moment forget about the IDE, and think of this sample json code, 
please fix it so that it does the following(this code is the minesweeper for my VBDOS clone IDE):
change all hte on_click events to on_button_up events please
make it so that when people click with the rigth mouse button, it shoudl make a flag with teh caption F on that button, if the caption is already there then clear it back to an empty string

I have included a file with the latest vesion of my IDE. please modify it and do the following:
if the user clicks on a window it should come to the foreground of the ohter windows, unless marked as pinned_topmost in hte form properties window as a checkbox. also, add a property to the propperties window of forms called "resizable widows" which shoudl be checked by default, but if this is not checked, the form should not be resizable.
add a parameter when i run the IDE, I ususallyy run it like this python3 rad-tui-py.py, but I want to be able to run it like this: python3 rad-tui-py.py -run script.json     , this shoudl run script.json in run mode.
also add a menu editing window in design mode only, which will allow me to define the top menu in runmode(but make sure the [stop] option is always visible too.the menu editor whould have an option to call teh code window, and input a function per each menu option as mnu_something_clicked() where somethign is the name of the menu option selected.some menu option should be allowed to have submenu options.


try to make the menu editor more visual, using existing components like a windows, and buttons isnetad of prompting for keypresses, also use combo boxes for each menu, and each submenu should ahve it's own combo box

that seems ok except for the buttons must have meaningful captions like Add menu , Delete menu and OK, you made all the button captions in the menu editor into "Button" , please fix that

ms2.json if a bomb explodes set the backround color of that button to red


add on_form_load() event to the form of the IDE, also when you double click on the background of the form, it should being up the code window with the definition of on_form_load()
chadge the menu editor so that the sub menus also appear in the top menu combo box. maybe with a small "..." as indentation to know it is a sub menu, and what level, I wnat htis so that sub menus can also h ave submenus. wil it work this way?
change the lable Top level menus to "selected menu" 
add 2 properties to text boxes, to enable horizontal and vertical scrollbars on the text box to be able to navigate large portions of text, for example in a text editor
make a calculator.json example file with a calculator simillar to the windwos calc.exe but for my IDE

add a function to load anohter form.
add a property for text boxes to highlight python code.
add a feature to select text in text areas using hte mouse.
make it so that if you right click a text area you get a pop up menu of copy and paste and select all.


make a text editor for my IDE, using a big size textbox with vertical and horizontal slidebars. the text area should highlight python code. make a file menu the file menu should ocntain sumbenues of Save, Save AS, give me an editor.json result


in the IDE, do the collowing:
add a feature that allows to hide a form.
add an event to the idea callen on_form_resize() which executes when the form is resized.
make the scroll wheel of hte mouse also scroll the text area the mouse is over.
make the tab key add 4 spaces indented into teh code of hte current text area if nothign is selected. but if something is selected have it add 4 spaces to every line that is selected.
make shift+tab remove 4 spaces form the current line if nothing is selected, but if anythign is selected make it remove 4 spaces from all the selected lines. 



also add to the IDE the ability to select text in the text areas by holding down the shift key and pressing any arrow key should allow to select text.


make a .json example, with a file dialog, if it is on windows show drives in a  combo box, ohterwise dont. make this dialogue pinned on top.


in the text editor json I am including here,
integrate the file dialogue for hte file picker, use the function to load anohter form we added earlier to load it form a json file called filedialogue.json. the save as shoudl bring up  the file dialogue that we made before and saves in a json file. also add a quit option ot the menu., when save or cancel is clickced hide the file picker.
make the menues actually do what they are supposed to, not just show msgboxes sayign what htey shoudl do, 
make the textarea adapt to teh size of the form when teh on_form_resize() function is called.


add a new menu under the latest editor.json(which I am including now), the menu should be Load , which loads a file opens a file in the text area.



I will include hte most new editor.json and filedialogue.json
the file load menu does not work... I want it to use filedialog.jason but when loaded it shoudl change  the caption  of the button from Save to Load, and it shoudl load the file and put it in the textarea, can  you make filedialogue usable bogth as Save as and Load dialogue depending on which menu called it please?


make the properties and tools windows be pinned to the top, cause I cant see them when the form is made really big
make the properties window expand down to fit all the properites available for hte current selected widget.
use editor.json as the editor for code in the IDE instead of the usual code editign window, when click save, instead of saving just go back to the design mode.
make the scrollwheel also scroll listsboxes up and down


use filedialog.json in the IDE the way you did in the editor to load projects and do save as for projects. have it default to the projects subdirectory of the project's directory. do the fixes to the IDE and give me the full listing of the IDE


add a "Save Project" option to the menu of the IDE, this should save the project with the same name as it was opened.
add a function to the IDE called END() which will end the program if executed form run, but will return to design mode if it is not run wiht -run.
if it is run wiht -run make the menu option [stop] dissapear
add to tesct boxes a method called .scroll_to_bottom() which will scroll the text box to the bottom. also add to text boxes a property called editable, which if unchecked the text box cant be edited.
do all this and then give me the full code to the IDE please

again I can no longer scroll on lists, please fix this
also [STOP] whould only be hidden in runtime when the -run parameter is set, adn nto when a program is loaded in the IDE form design time


remove Dir list, Drive list, File List, Picture Box, form toolbox and resize toolbox properly 
enable Hscrolbar, as a scrolls bar, enabel Vscrollbar as a scrollbar. then give me hte full listing of hte IDE code



add an on_change_crtlName() event to text boxes, which shoudl trigger every time the text chages in a text box
add a menu option to the IDE, under the edit option, to remove a control form the form, it shoudl remove the control that is selected
do all this and then give me the full code to the IDE please

remove View and Options menu items form IDE.

add an event on_selection_start() and on_selection_end() to the text box . 
include a mouse objext which should have an x and y properties so I can do x=mouse.x and y=mouse.y to get the current coordinates of the mouse
and give me the full code ot the IDE also

feature for editor.json now called tsukino.json, add in submenu when rigth clicking, comment likes and uncomment lines, which shoudl comment all lines selected and uncommet all lines selected by adding  or removing # fromt he beginning of every line selected, uploading tsukino.json, give me the full new code for tsukino.json

in rad-tui-ide.py, after loading a project or saving a project, in the dialogue box, a [ok] button by itself appears on hte screen, please take away that ok warning. also the [ok] button appears after clicking the save project menu, remove that too.
implement ctrl+a to select all in text box , ctrl+c to copy in text box and ctrl+v to paste in text box
please fix all this adn give me the full code to rad-tui-ide.py

--done

please make it so that if we invoke rad-tui-IDE.py -run tsukino.json with no parapeter, it should start a new file called untitled.py and if we start it with a file that does not exist, it created the file before opening it.
add a new command line option called -dark-mode which shoudl set the whole look to darkmode.
add the ability for list boxes to have options properties in different backgroudn adn foregrroud colors

do I have the nessesary controls to make a clone of lotus123 in my IDE? if I do please go ahead and make it
